你正在为一个 A 股日线截面 alpha 因子的量化回测系统做因子进化（演化式自动挖掘）。

任务：对下方【单个父代因子】做一次【定向变异】，创造 <<N>> 个全新的因子文件。变异遵循以下原则
（参考 MadEvolve 演化框架）：
- 多数情况下做【局部、定向】的改动：在保留父代核心逻辑/结构的前提下，只改其中一处或几处——
  例如换一种归一化/标准化方式、调整某个比率或交互项、加入稳健化（去极值/截面 z-score/符号变换）、
  替换或叠加一个信号源、改变时间窗口的组合方式等。不要只改一个常数。
- 少数情况下（约三成）可做更大胆的【结构性重写】，提出与父代不同的全新表达，追求更高夏普与多样性。
- 参考父代的训练表现（夏普）判断改进方向：表现差就大改，表现好就在其基础上精修增强。

<<CONTRACT>>

父代因子（你要变异的对象，含训练期夏普）：
<<PARENTS>>

灵感参考（库中现有优秀因子，仅供启发，禁止照抄或微调复制）：
<<INSPIRATIONS>>

输出要求：
- 用 Write 工具在【当前目录】创建每个文件。
- 文件名与类名必须严格等于：<<NAMES>>
  （一个名字一个文件，文件 `<Name>.py` 内含 `class <Name>:`）。
- 每个因子都要是单一、纯 numpy、向量化的截面因子，且与父代、与灵感因子都【显著不同】。
- 只写因子文件本身。不要运行任何东西，不要创建其它文件。
