""" 配置各个因子的权重和温度参数 """

# 因子权重（用于加权求和）
FactorWeights = {
    'MACD': 1.0,
    'BBI': 0.5,
    'CCI': 0.5,
    # 'Fundamental': 1.5,  # 基本面因子（已禁用 - API调用慢）
    'TRIXFactor': 0.8,   # 三重指数平滑因子
    'MOMFactor': 0.6,    # 动量因子
    'ADXFactor': 0.7,    # 趋势强度因子
}

# 因子温度参数（用于归一化平滑）
# - T < 1.0: 压缩差异，排名更稳定（适合长期因子）
# - T = 1.0: 保持原始差异
# - T > 1.0: 放大差异，排名更灵敏（适合短期因子）
FactorTemperatures = {
    'MACD': 0.8,         # 中期因子，稍微平滑
    'BBI': 0.7,          # 长期因子，较平滑
    'CCI': 0.6,          # 长期因子，较平滑
    # 'Fundamental': 0.5,  # 基本面因子（已禁用 - API调用慢）
    'TRIXFactor': 0.8,   # 三重指数平滑因子，稍微平滑
    'MOMFactor': 1.0,    # 动量因子，保持原始差异
    'ADXFactor': 0.7,    # 趋势强度因子，较平滑
}

# 选股数量
RANK_N = 20
